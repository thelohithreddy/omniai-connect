"""Interface adapters — protocol surfaces over the canonical domains (MCP_RUNTIME §1).

Adapters translate protocol messages to domain/runtime calls and nothing more; policy,
authorization, credential handling, and execution stay in the domains and the Runtime.
"""
